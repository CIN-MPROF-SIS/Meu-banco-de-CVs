<%= form_for @questionario do |f| %>
  <% if @questionario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questionario.errors.count, "error") %> prohibited this questionario from being saved:</h2>

      <ul>
      <% @questionario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.hidden_field :vaga_id %>
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <%= f.fields_for :questoes do |questao| %>
    <%= render "questao_fields", :f => questao %>
  <% end %>
  <p><%= link_to_add_fields "Add Questão", f, :questoes %></p>
  <p><%= f.submit "Submit" %></p>
<% end %>
