 <%= form_for :moderador, url: moderadores_path do |f| %>
  <% if @moderador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moderador.errors.count, "error") %> prohibited this moderador from being saved:</h2>

      <ul>
      <% @moderador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :login %><br>
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= f.label :senha %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
