<%= form_for(@unidade_federativa) do |f| %>
  <% if @unidade_federativa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unidade_federativa.errors.count, "error") %> prohibited this unidade_federativa from being saved:</h2>

      <ul>
      <% @unidade_federativa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sigla %><br>
    <%= f.text_field :sigla %>
  </div>
  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
