<p id="notice"><%= notice %></p>

<h1>Listing Unidades Federativas</h1>

<table>
  <thead>
    <tr>
      <th>Sigla</th>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @unidades_federativas.each do |unidade_federativa| %>
      <tr>
        <td><%= unidade_federativa.sigla %></td>
        <td><%= unidade_federativa.nome %></td>
        <td><%= link_to 'Show', unidade_federativa %></td>
        <td><%= link_to 'Edit', edit_unidade_federativa_path(unidade_federativa) %></td>
        <td><%= link_to 'Destroy', unidade_federativa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Unidade federativa', new_unidade_federativa_path %>
