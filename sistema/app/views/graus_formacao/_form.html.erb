<%= form_for(@grau_formacao) do |f| %>
  <% if @grau_formacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grau_formacao.errors.count, "error") %> prohibited this grau_formacao from being saved:</h2>

      <ul>
      <% @grau_formacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
