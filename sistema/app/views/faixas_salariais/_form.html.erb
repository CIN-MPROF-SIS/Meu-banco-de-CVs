<%= form_for(@faixa_salarial) do |f| %>
  <% if @faixa_salarial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faixa_salarial.errors.count, "error") %> prohibited this faixa_salarial from being saved:</h2>

      <ul>
      <% @faixa_salarial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :valor %><br>
    <%= f.text_field :valor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
